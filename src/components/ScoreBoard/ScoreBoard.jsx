import React from 'react'
// import PropTypes from 'prop-types'

const ScoreBoard = ({type}) => {
    return (
        <div className={type}>
            This goes in the Right container
        </div>
    )
}

// ScoreBoard.propTypes = {

// }

export default ScoreBoard
