


const TicGameContainer = () => {

 

    



    return ""

    //(component, player, computer, second, vsPlayer, vsComputer)

//     if (window.innerWidth<=800) return smallWindowMount(gameWindow, vsPlayer, vsComputer);
//     else return largeWindowMount(gameWindow, playerPoints, computerPoints, second, vsPlayer, vsComputer);
}

export default TicGameContainer
